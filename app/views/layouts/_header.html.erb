<div><%= image_tag('background3.jpg', width: "100%")%></div>
<nav class="navbar navbar-inverse">
  <div class="container-fluid"></div>
  
    <div class="navbar-header">
      
      <ul class="nav nav-pills navbar-left">
        <li class="navbar_li"><%= link_to "Home", root_path, class: "btn btn-success"%></li>
        <% if user_signed_in? %>
          <li><%= link_to "Profile", current_user, class: "btn btn-default"%></li>
          <% if current_user.admin?%>
            <li class="navbar_li"><%= link_to "Admin Side", admin_main_path, class: "btn btn-success" %></li>
          <% end %>
        <% else %>
          <li class="navbar_li"><%= link_to "Sign Up!", new_user_registration_path, class: "btn btn-danger"%></li>
          <li class="navbar_li"><%= link_to "Sign in", new_user_session_path, class: "btn btn-warning"%></li>
        <% end %>
        <% if user_signed_in? %>
          <li><%= link_to "Logout", destroy_user_session_path, method: "delete", class: "btn btn-default" %></li>
        <% end %>
      </ul>
    </div>
    <div class="nav navbar-nav navbar-right"><%= link_to "#{current_order.order_items.size} Items in Cart ( #{number_to_currency current_order.subtotal} )", cart_path, class: "btn-lg btn-danger"%></div>

    </div>
    

  </div>
</nav>